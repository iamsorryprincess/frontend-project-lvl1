name: Node CI

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run install
        run: |
          make install
      - name: Run linter
        run: |
          make lint
